<UserControl
    x:Class="LuaToolsGui.Views.TorrentDownloadsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:LuaToolsGui.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:LuaToolsGui.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:TorrentDownloadsViewModel}"
    TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid Margin="8,16,16,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock FontSize="22" FontWeight="Bold" Text="{x:Static res:Strings.Nav_Downloads}" />
                <TextBlock
                    Margin="0,2,0,0"
                    FontSize="13"
                    Foreground="#9ca3af"
                    Text="{x:Static res:Strings.Downloads_Subtitle}"
                    TextWrapping="Wrap" />
            </StackPanel>

            <ui:Button
                Grid.Column="1"
                Margin="16,0,0,0"
                VerticalAlignment="Center"
                Appearance="Secondary"
                Command="{Binding ClearCompletedCommand}"
                Content="{x:Static res:Strings.Downloads_ClearCompleted}"
                Icon="{ui:SymbolIcon Delete24}"
                Visibility="{Binding HasCompletedDownloads, Converter={StaticResource BoolToVis}}" />
        </Grid>

        <Grid Grid.Row="1" Margin="0,18,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Downloads}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Margin="0,0,0,10"
                                Padding="16,14"
                                Background="#0DFFFFFF"
                                BorderBrush="#1FFFFFFF"
                                BorderThickness="1"
                                CornerRadius="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock FontSize="14" FontWeight="SemiBold" Text="{Binding Name}" />
                                        <Border Margin="8,0,0,0" Padding="6,2" Background="#22A78BFA" CornerRadius="4">
                                            <TextBlock FontSize="10" Foreground="#c4b5fd" Text="{Binding Source}" />
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Grid.RowSpan="4" Margin="16,0,0,0" VerticalAlignment="Center">
                                        <ui:Button
                                            Margin="0,0,0,6"
                                            Appearance="Secondary"
                                            Command="{Binding CancelCommand}"
                                            Content="{x:Static res:Strings.Add_Kazumi_Cancel}"
                                            Icon="{ui:SymbolIcon DismissCircle24}"
                                            Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}" />
                                        <ui:Button
                                            Appearance="Secondary"
                                            Command="{Binding OpenFolderCommand}"
                                            Content="{x:Static res:Strings.Settings_Open}"
                                            Icon="{ui:SymbolIcon FolderOpen24}" />
                                    </StackPanel>

                                    <TextBlock
                                        Grid.Row="1"
                                        Margin="0,7,0,0"
                                        Foreground="#9ca3af"
                                        Text="{Binding StatusText}" />

                                    <ProgressBar
                                        Grid.Row="2"
                                        Height="5"
                                        Margin="0,10,0,0"
                                        Maximum="100"
                                        Value="{Binding Progress}" />

                                    <TextBlock
                                        Grid.Row="3"
                                        Margin="0,8,0,0"
                                        FontSize="11.5"
                                        Foreground="#6b7280"
                                        Text="{Binding DestinationFolder}"
                                        TextTrimming="CharacterEllipsis"
                                        ToolTip="{Binding DestinationFolder}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding HasDownloads, Converter={StaticResource InverseBoolToVis}}">
                <ui:SymbolIcon HorizontalAlignment="Center" FontSize="34" Foreground="#6b7280" Symbol="ArrowDownload24" />
                <TextBlock
                    Margin="0,10,0,0"
                    Foreground="#9ca3af"
                    Text="{x:Static res:Strings.Downloads_Empty}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
